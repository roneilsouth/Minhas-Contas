name: Build APK Android

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Create package.json
        run: |
          cat > package.json << 'EOF'
          {
            "name": "minhascontas",
            "version": "1.0.0",
            "private": true
          }
          EOF

      - name: Install Capacitor
        run: npm install @capacitor/core @capacitor/android @capacitor/cli

      - name: Init Capacitor
        run: npx cap init MinhasContas com.minhascontas.app

      - name: Add Android platform
        run: npx cap add android

      - name: Set webDir to root
        run: |
          cat > capacitor.config.json << 'EOF'
          {
            "appId": "com.minhascontas.app",
            "appName": "Minhas Contas",
            "webDir": ".",
            "bundledWebRuntime": false
          }
          EOF

      - name: Sync
        run: npx cap sync android

      - name: Build APK
        run: cd android && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MinhasContas-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
