{
  "name": "crm-financeiro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp index.html www/index.html"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
